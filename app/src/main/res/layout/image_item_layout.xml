<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/relativeLayoutPreview"
    android:layout_width="wrap_content" android:layout_height="wrap_content"
    android:background="@color/gris"
    >

    <ImageView
        android:id="@+id/imageViewPreview"
        android:layout_width="@dimen/checklist_preview_evidenica_size"
        android:layout_height="@dimen/checklist_preview_evidenica_size"
        android:layout_margin="@dimen/checklist_preview_evidenica_margin"/>

</RelativeLayout>